# -*- mode: snippet -*-
# name: arr.indexOf(elt, fromIndex)
# key: iof
# uuid: iof
# --
`(unless (eq (char-before) ?.) ".")`indexOf(${1:`(or (doom-snippets-text nil t) "elt")`}, ${2:fromIndex})`(when (and (bound-and-true-p my/semi) (eolp)) ";")`