# -*- mode: snippet -*-
# contributor: Seong Yong-ju
# name: Redux container
# key: container
# --
import { connect } from 'react-redux'`(doom-snippets-maybe-semi)`

const mapStateToProps = (state, ownProps) => ({
  $0
})`(doom-snippets-maybe-semi)`

const mapDispatchToProps = (dispatch, ownProps) => ({

})`(doom-snippets-maybe-semi)`

const ${1:`(f-base buffer-file-name)`} = connect(
  mapStateToProps,
  mapDispatchToProps
)(${2:Component})`(doom-snippets-maybe-semi)`

export default $1`(doom-snippets-maybe-semi)`