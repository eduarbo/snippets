# -*- mode: snippet -*-
# contributor: Seong Yong-ju
# name: reducer
# key: reducer
# --
const initialState = {
  $1
}`(doom-snippets-maybe-semi)`

export default (state = initialState, action) => {
  switch (action.type) {
    $0

    default:
      return state`(doom-snippets-maybe-semi)`
  }
}`(doom-snippets-maybe-semi)`
