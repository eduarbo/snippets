# -*- mode: snippet -*-
# contributor: Seong Yong-ju
# name: reducer
# key: reducer
# --
const initialState = {
  $1
}`(when (bound-and-true-p my/semi) ";")`

export default (state = initialState, action) => {
  switch (action.type) {
    $0

    default:
      return state`(when (bound-and-true-p my/semi) ";")`
  }
}`(when (bound-and-true-p my/semi) ";")`
